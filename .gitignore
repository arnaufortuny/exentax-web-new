node_modules/
dist/
.env
*.log
.DS_Store
.vite/
**/service-account*.json
**/service-account*.txt
**/Pasted--type-service-account*
attached_assets/

# Auto-generated by lint:blog (timestamp churn — see Task #5 rebase)
exentax-web/docs/auditoria-multiidioma/blog-translation-quality-ext.json
exentax-web/docs/auditoria-multiidioma/blog-translation-quality-ext.md

# State persisted by .github/workflows/live-verification.yml between cron runs
# (Task #56). Never committed: it lives in actions/cache only.
.live-verification-state/
docs/internal/live-verification-latest.md
.check.pid
exentax-web/.check.pid

# IDE / platform metadata that should not pollute commits if regenerated.
# Existing tracked files under these paths stay tracked (.gitignore only
# affects new entries) — see audit ronda 6 for context.
.canvas/
.agents/
.lighthouseci/
